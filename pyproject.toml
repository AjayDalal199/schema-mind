[project]
name = "schema-mind"
version = "0.1.0"
description = "Backend-first OCR and schema-based extraction service"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.4",
    "pydantic-settings>=2.12.0",
    "uvicorn>=0.40.0",
]
